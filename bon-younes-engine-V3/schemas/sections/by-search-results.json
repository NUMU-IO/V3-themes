{
  "type": "by-search-results",
  "name": "Search results",
  "settings": [
    { "type": "text", "id": "placeholder_when_no_query", "label": "Empty-query headline", "default": "Search the menu — try 'latte' or 'mango'." },
    { "type": "textarea", "id": "no_results_text", "label": "No-results copy", "default": "No matches. Try a different keyword or browse all drinks." },
    { "type": "range", "id": "columns_desktop", "label": "Columns (desktop)", "min": 1, "max": 5, "step": 1, "default": 3 }
  ]
}
